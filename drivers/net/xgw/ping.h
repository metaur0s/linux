
// KEYS                     SENDER'S IKEYS BEING TAUGHT (UNDEFINED ON PONG)
// RANDOM | VERSION         SENDER'S IKEYS INDEX BEING REGISTERED (UNDEFINED ON PONG)
// CTR                      SENDER'S LCOUNTER
#define PING_SIZE 80

// KEYS, VERSION
#define PING_RANDOMS_N 9

// NOTE: AO ENVIAR UM PONG, O PKT->COUNTER É O RCOUNTER SENDO RESPONDIDO

#define PKT_PING_K(pkt)     (pkt->p + PKT_ALIGN_RANDOMS)
#define PKT_PING_VER(pkt)   (pkt->p[PKT_ALIGN_RANDOMS + KEYS_N])
#define PKT_PING_CTR(pkt)   (pkt->p[PKT_ALIGN_RANDOMS + KEYS_N + 1])
